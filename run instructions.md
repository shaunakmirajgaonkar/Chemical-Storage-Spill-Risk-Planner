# Run Instructions

```bash
cd Chemical_Storage_Spill_Risk_Planner
python3 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
pip install -r requirements.txt
streamlit run app.py
```
